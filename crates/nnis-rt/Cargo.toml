[package]
name = "nnis-rt"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Safe NVIDIA driver-runtime layer for NNIS: devices, contexts, streams, events, memory"

[dependencies]
nnis-sys = { workspace = true }
