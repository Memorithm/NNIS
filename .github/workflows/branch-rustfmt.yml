name: Branch rustfmt

on:
  push:
    branches:
      - feat/nnml0-capability-bound-evidence

permissions:
  contents: write

jobs:
  rustfmt:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: feat/nnml0-capability-bound-evidence
          fetch-depth: 0

      - name: Install rustfmt
        run: rustup component add rustfmt

      - name: Format workspace
        run: cargo fmt --all

      - name: Commit formatting when needed
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add crates/nnis-model/examples/nnml0_real_safetensors.rs
          git commit -m "style(nnml0): apply rustfmt to capability evidence"
          git push origin HEAD:feat/nnml0-capability-bound-evidence
